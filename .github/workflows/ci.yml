name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install ruff
      - run: ruff check .
      - run: ruff format --check .

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: prefix-dev/setup-pixi@v0.10.1
        with:
          # Must be able to read pixi.lock's format (v6); v0.41.4 could not.
          pixi-version: v0.72.2
          environments: ci-test
          cache: true
      - name: Run tests
        run: pixi run -e ci-test pytest --tb=short -v
